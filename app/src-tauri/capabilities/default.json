{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the local splash webview: core defaults plus the server supervisor commands used on first run.",
  "windows": ["main"],
  "webviews": ["main"],
  "permissions": [
    "core:default",
    "allow-server-control"
  ]
}
