{
  "name": "@idl3/claude-control",
  "version": "1.10.0",
  "type": "module",
  "description": "Local web UI to watch and drive your Claude Code sessions running in tmux — live transcripts, reply, answer AskUserQuestion, attach files, from a browser or phone.",
  "keywords": [
    "claude",
    "claude-code",
    "tmux",
    "dashboard",
    "agent"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/idl3/claude-control.git"
  },
  "homepage": "https://github.com/idl3/claude-control#readme",
  "bin": {
    "claude-control": "bin/cli.js"
  },
  "files": [
    "server.js",
    "lib/",
    "hooks/",
    "scripts/",
    "public/",
    "web/dist/",
    "bin/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test --test-concurrency=2",
    "eval:optimise": "node scripts/eval-optimize.mjs",
    "cleanup:stale-dev": "node scripts/cleanup-stale-dev.mjs",
    "cleanup:stale-dev:apply": "node scripts/cleanup-stale-dev.mjs --apply",
    "install-hook": "node scripts/install-pane-hook.mjs",
    "build:web": "cd web && npm install && npm run build",
    "build": "npm run build:web",
    "prepack": "npm run build && find web/dist -name '*.map' -type f -delete"
  },
  "engines": {
    "node": ">=20"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "web-push": "^3.6.7",
    "ws": "^8.18.0"
  }
}
